import type { NextPage } from "next";
import Head from "next/head";
import { BasicsView, CharactbitView } from "../views";

const Charactbit: NextPage = (props) => {
  return (
    <div>
        <CharactbitView/>
        
    </div>
  );
};

export default Charactbit;
